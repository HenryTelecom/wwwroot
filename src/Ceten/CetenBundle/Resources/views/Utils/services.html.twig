            <div class="ceten-services">
                <nav class="navbar navbar-default" role="navigation">
                    <div class="container-fluid">
                        <!-- Brand and toggle get grouped for better mobile display -->
                        <div class="navbar-header">
                            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#ceten-services-collapse">
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                            </button>

                            <ul class="nav navbar-nav">
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle navbar-brand" data-toggle="dropdown">
                                        <img src="{{ cdn('/assets/images/logo/ceten-uni-16x16.png') }}" alt="" title="Cercles des Élèves de TELECOM Nancy" class="ceten-network-icon" >CETEN Network
                                    </a>
                                    <ul class="dropdown-menu" role="menu">
{% for service in ceten.services %}
                                        <li><a href="{{ external(service.url) }}">{{ service.label }}</a></li>
{% endfor %}
                                    </ul>
                                </li>
                            </ul>
                        </div>

                        <div class="collapse navbar-collapse" id="ceten-services-collapse">
                            <ul class="nav navbar-nav navbar-right">
{% if is_granted('ROLE_USER') %}
                                <li><a href="{{ oauth('/profile') }}">{{ app.user.username }}</a></li>
                                <li><a href="{{ oauth('/signout') ~ service_name }}">Déconnexion</a></li>
{% else %}
                                <li><a href="{{ oauth('/signin') ~ service_name }}">Connexion</a></li>
{% endif %}
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>